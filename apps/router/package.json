{
	"name": "microfrontend-template",
	"description": "Route requests to different Workers based on path patterns with automatic URL rewriting for unified microfrontend applications.",
	"cloudflare": {
		"label": "Microfrontend Router",
		"products": [
			"Workers"
		],
		"categories": [
			"starter"
		]
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "0.21.3",
		"@cloudflare/workers-types": "^5.20260920.1",
		"@types/node": "^26.6.2",
		"@vitest/runner": "4.1.10",
		"@vitest/snapshot": "4.1.10",
		"typescript": "5.9.3",
		"vitest": "4.1.10",
		"wrangler": "^4.135.0"
	},
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"e2e:dev": "node e2e/dev.mjs",
		"test": "vitest run",
		"types": "tsc --noEmit"
	}
}
