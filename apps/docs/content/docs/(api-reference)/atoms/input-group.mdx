---
title: InputGroup
description: "Visual composition of several inputs and addons."
---

<PhonePreview>
	<div class="ax-appbar">
		<span class="ax-icon-btn">
			<Glyph name="chevron-left" />
		</span>
		<span class="ax-appbar-title">New link</span>
		<span class="ax-icon-btn" />
	</div>
	<div class="ax-body" style={{ gap: "18px" }}>
		<div class="ax-field">
			<span class="ax-label">Website</span>
			<div class="ax-input-group" data-state="focused">
				<span class="ax-addon">https://</span>
				<span class="ax-grow ax-caret">axiom.dev</span>
			</div>
		</div>
		<div class="ax-field">
			<span class="ax-label">Phone</span>
			<div class="ax-input-group">
				<span class="ax-addon">
					🇫🇷 +33
					<Glyph name="chevron-down" size={14} />
				</span>
				<span class="ax-grow">6 12 34 56 78</span>
			</div>
		</div>
		<div class="ax-field">
			<span class="ax-label">Weight</span>
			<div class="ax-input-group">
				<span class="ax-grow">72</span>
				<span class="ax-addon">kg</span>
			</div>
		</div>
		<div class="ax-field">
			<span class="ax-label">Invite</span>
			<div class="ax-input-group">
				<span class="ax-grow ax-subtle">name@company.com</span>
				<span class="ax-btn" data-size="sm">
					Send
				</span>
			</div>
		</div>
	</div>
</PhonePreview>

## Installation

```npm
npx axiom add input-group
```

Also copies [`input`](/atoms/input), whose tokens draw the group border and text, and [`button`](/atoms/button).

## What it does

`InputGroup` places inputs and addons (a currency label, a unit, a button) side by side so they read as one field. The group draws the border; the inputs inside drop theirs.

## Usage

```tsx
import { InputGroup } from "@/components/ui/input-group";

<InputGroup>
	<InputGroup.Addon>https://</InputGroup.Addon>
	<InputGroup.Input placeholder="example.com" />
</InputGroup>;
```

## Props

### InputGroup

| Prop       | Type                   | Default | Description                                                                                           |
| ---------- | ---------------------- | ------- | ----------------------------------------------------------------------------------------------------- |
| `children` | `ReactNode`            | —       | Any mix of `InputGroup.Input`, `InputGroup.Addon` and `InputGroup.Button`.                            |
| `size`     | `'sm' \| 'md' \| 'lg'` | `'md'`  | Minimum height of the whole group, 32, 44 or 52pt; children follow it. Grows with larger system text. |
| `error`    | `boolean`              | `false` | Red border around the whole group.                                                                    |
| `disabled` | `boolean`              | `false` | Disables every input and button in the group.                                                         |
| `divided`  | `boolean`              | `true`  | Draws a separator between adjacent parts.                                                             |
| `style`    | `StyleProp<ViewStyle>` | —       | Extra styles for the container.                                                                       |

The focused border is drawn around the whole group as soon as one of its inputs has focus.

### InputGroup.Input

Every [Input](/atoms/input) prop except `label`, `helper`, `error` and `size`, which belong to the group or the surrounding form.

| Prop       | Type      | Default | Description                                                                                              |
| ---------- | --------- | ------- | -------------------------------------------------------------------------------------------------------- |
| `flex`     | `number`  | `1`     | Share of the remaining width. Two inputs with `flex={1}` and `flex={2}` split it one third / two thirds. |
| `disabled` | `boolean` | `false` | Disables this input only. The group's `disabled` disables all of them.                                   |

Without a visible label, set `accessibilityLabel`; the placeholder is used otherwise.

### InputGroup.Addon

| Prop                 | Type                  | Default      | Description                                                                                      |
| -------------------- | --------------------- | ------------ | ------------------------------------------------------------------------------------------------ |
| `children`           | `ReactNode`           | —            | Static text or an icon.                                                                          |
| `onPress`            | `() => void`          | —            | Makes the addon pressable, for example to open a country picker.                                 |
| `variant`            | `'subtle' \| 'plain'` | `'subtle'`   | `subtle` fills the addon with `inputGroup.default.default.addon`, `plain` leaves it transparent. |
| `accessibilityLabel` | `string`              | text content | Announced when the addon is pressable.                                                           |

### InputGroup.Button

A [Button](/atoms/button) with its radius and border removed so it sits flush in the group. Same props as `Button`, except `size`, `fullWidth` and `asChild`: the size follows the group.

## Use cases

### Phone number with a country picker

The addon is pressable and opens a [BottomSheet](/atoms/bottom-sheet) with the list of countries.

<PhonePreview size="sm">
	<div class="ax-body" style={{ gap: "16px", paddingTop: "24px" }}>
		<span class="ax-title1">Your number</span>
		<span class="ax-subhead ax-muted">
			We'll text you a code to confirm it.
		</span>
		<div
			class="ax-input-group"
			data-state="focused"
			style={{ height: "52px" }}
		>
			<span class="ax-addon">
				🇬🇧 +44
				<Glyph name="chevron-down" size={14} />
			</span>
			<span class="ax-grow ax-caret">7700 900123</span>
		</div>
		<span class="ax-btn" data-size="lg" data-block>
			Send code
		</span>
	</div>
</PhonePreview>

```tsx
<InputGroup size="lg">
	<InputGroup.Addon onPress={openCountries}>
		{country.flag} {country.dialCode}
	</InputGroup.Addon>
	<InputGroup.Input
		keyboardType="phone-pad"
		autoComplete="tel"
		value={phone}
		onChangeText={setPhone}
	/>
</InputGroup>
```

### Field with an inline action

A button inside the group for an action that only concerns this value: copy, apply, send.

<PhonePreview size="sm">
	<div class="ax-appbar">
		<span class="ax-icon-btn">
			<Glyph name="chevron-left" />
		</span>
		<span class="ax-appbar-title">Checkout</span>
		<span class="ax-icon-btn" />
	</div>
	<div class="ax-body" style={{ gap: "14px" }}>
		<div class="ax-row" data-justify="between">
			<span class="ax-muted">Subtotal</span>
			<span>$64.00</span>
		</div>
		<div class="ax-field">
			<span class="ax-label">Promo code</span>
			<div class="ax-input-group">
				<span class="ax-grow">SPRING20</span>
				<span
					class="ax-btn"
					data-size="sm"
					data-variant="ghost"
					style={{ color: "var(--ax-content-link)" }}
				>
					Apply
				</span>
			</div>
		</div>
		<div class="ax-alert" data-variant="success">
			<Glyph name="circle-check" size={20} />
			<span class="ax-subhead">20% off applied: −$12.80</span>
		</div>
	</div>
</PhonePreview>

```tsx
<InputGroup>
	<InputGroup.Input
		autoCapitalize="characters"
		value={code}
		onChangeText={setCode}
	/>
	<InputGroup.Button variant="ghost" onPress={applyCode}>
		Apply
	</InputGroup.Button>
</InputGroup>
```

### Two inputs as one field

A range, or an expiry date and a CVC: separate values the user reads as one line.

<PhonePreview size="sm">
	<div class="ax-appbar">
		<span class="ax-icon-btn">
			<Glyph name="close" />
		</span>
		<span class="ax-appbar-title">Add card</span>
		<span class="ax-icon-btn" />
	</div>
	<div class="ax-body" style={{ gap: "14px" }}>
		<div class="ax-field">
			<span class="ax-label">Card number</span>
			<div class="ax-control">
				<Glyph name="card" size={20} />
				<span class="ax-value">4242 4242 4242 4242</span>
			</div>
		</div>
		<div class="ax-field">
			<span class="ax-label">Expiry and CVC</span>
			<div class="ax-input-group" data-state="focused">
				<span class="ax-grow">08 / 29</span>
				<span class="ax-grow ax-caret">12</span>
			</div>
		</div>
	</div>
</PhonePreview>

```tsx
<InputGroup>
	<InputGroup.Input placeholder="MM / YY" keyboardType="number-pad" />
	<InputGroup.Input
		placeholder="CVC"
		keyboardType="number-pad"
		secureTextEntry
	/>
</InputGroup>
```

## Related

- [Input](/atoms/input)
- [ButtonGroup](/atoms/button-group)
