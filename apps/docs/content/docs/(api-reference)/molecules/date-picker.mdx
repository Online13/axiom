---
title: DatePicker
description: "Date selection built from Calendar and BottomSheet."
---

<PhonePreview half="bottom">
  <div class="ax-body" style={{ gap: '12px' }}>
    <div class="ax-field"><span class="ax-label">Departure</span><div class="ax-control" data-state="focused"><Glyph name="calendar" size={20} /><span class="ax-value">Thu 17 Sep 2026</span></div></div>
  </div>
  <div class="ax-scrim" />
  <div class="ax-sheet">
    <span class="ax-handle" />
    <div class="ax-sheet-header"><span class="ax-link">Cancel</span><span class="ax-headline">Departure</span><span class="ax-link ax-semibold">Done</span></div>
    <div class="ax-calendar">
      <div class="ax-calendar-head"><span class="ax-headline">September 2026<Glyph name="chevron-right" size={16} class="ax-link" style={{ verticalAlign: '-3px', marginLeft: '4px' }} /></span><div class="ax-row" data-gap="4"><span class="ax-icon-btn" data-size="sm"><Glyph name="chevron-left" size={20} class="ax-link" /></span><span class="ax-icon-btn" data-size="sm"><Glyph name="chevron-right" size={20} class="ax-link" /></span></div></div>
      <div class="ax-calendar-grid">
        <span class="ax-weekday">Mon</span><span class="ax-weekday">Tue</span><span class="ax-weekday">Wed</span><span class="ax-weekday">Thu</span><span class="ax-weekday">Fri</span><span class="ax-weekday">Sat</span><span class="ax-weekday">Sun</span>
        <span data-today>14</span><span>15</span><span>16</span><span data-selected><i>17</i></span><span>18</span><span>19</span><span>20</span>
        <span>21</span><span>22</span><span>23</span><span>24</span><span>25</span><span>26</span><span>27</span>
      </div>
    </div>
  </div>
</PhonePreview>

## Installation

```npm
npx axiom add date-picker
```

Also copies: [`calendar`](/atoms/calendar), [`bottom-sheet`](/atoms/bottom-sheet), [`button`](/atoms/button), [`chip`](/atoms/chip), [`input`](/atoms/input) and their dependencies.

Requires the `calendar` icon in your [icon registry](/atoms/icon#icons-that-components-need). The field borrows the colors of [Input](/atoms/input): DatePicker brings no tokens of its own.

## Usage

```tsx
import { DatePicker } from '@/components/ui/date-picker';

<DatePicker label="Departure" value={date} onChange={setDate} />
```

## How it's built

```text
          date-picker
               │
       ┌───────┴───────┐
       ↓               ↓
   calendar      bottom-sheet
```

The field opens a sheet containing a calendar, so the user picks a date without leaving the screen.

## Props

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `mode` | `'single' \| 'range'` | `'single'` | One date, or a start and end date. Passed to the [Calendar](/atoms/calendar). |
| `value` | `Date \| { from: Date; to?: Date } \| null` | — | The selected date or range. `null` shows the placeholder. |
| `onChange` | `(value) => void` | — | Called with the new value when the user confirms. |
| `confirm` | `'done' \| 'instant'` | `'done'` | `done`: the value changes on Done, Cancel reverts it. `instant`: the sheet closes on the first press (single mode only). |
| `label` | `string` | — | Label above the field, like [Input](/atoms/input). |
| `placeholder` | `string` | `'Select a date'` | Text in the empty field. |
| `helper` | `string` | — | Note under the field, like [Input](/atoms/input). |
| `error` | `string \| boolean` | — | Puts the field in the `invalid` state. A string also replaces `helper`. |
| `format` | `(value) => string` | locale medium date | How the value is written in the field, e.g. "Thu 17 Sep 2026". |
| `title` | `string` | `label` | Title of the sheet. |
| `minDate` / `maxDate` | `Date` | — | Limits, passed to the calendar. |
| `isDateDisabled` | `(date: Date) => boolean` | — | Disabled days, passed to the calendar. |
| `presets` | `Array<{ label: string; value: Date \| Range }>` | — | Shortcut chips above the calendar: "Today", "Next weekend". |
| `clearable` | `boolean` | `false` | Adds a Clear action in the sheet that sets the value to `null`. |
| `disabled` | `boolean` | `false` | The field can't open the sheet. |
| `trigger` | `ReactElement` | input field | Replaces the default field, for example with a [Chip](/atoms/chip) or a [Button](/atoms/button). |
| `calendarProps` | `Partial<CalendarRootProps>` | — | Any other `Calendar.Root` prop, like `weekStartsOn` or `minRange`. |
| `renderCalendar` | `() => ReactNode` | full calendar | Children of the `Calendar.Root` inside the sheet, to change its parts: event dots, a custom header. |

## Use cases

### Date of a task

`confirm="instant"`: one press sets the date and closes the sheet. Presets cover the common choices.

<PhonePreview half="bottom" size="sm">
  <div class="ax-scrim" />
  <div class="ax-sheet">
    <span class="ax-handle" />
    <div class="ax-sheet-header"><span class="ax-title3">Due date</span><span class="ax-link">Clear</span></div>
    <div class="ax-row" style={{ padding: '4px 0 10px' }}><span class="ax-chip" data-size="sm">Today</span><span class="ax-chip" data-size="sm" data-selected>Tomorrow</span><span class="ax-chip" data-size="sm">Next week</span></div>
    <div class="ax-calendar-grid">
      <span class="ax-weekday">M</span><span class="ax-weekday">T</span><span class="ax-weekday">W</span><span class="ax-weekday">T</span><span class="ax-weekday">F</span><span class="ax-weekday">S</span><span class="ax-weekday">S</span>
      <span data-today>14</span><span data-selected><i>15</i></span><span>16</span><span>17</span><span>18</span><span>19</span><span>20</span>
      <span>21</span><span>22</span><span>23</span><span>24</span><span>25</span><span>26</span><span>27</span>
    </div>
  </div>
</PhonePreview>

```tsx
<DatePicker
  confirm="instant"
  clearable
  value={task.due}
  onChange={(due) => update({ due })}
  presets={[
    { label: 'Today', value: today },
    { label: 'Tomorrow', value: addDays(today, 1) },
    { label: 'Next week', value: nextMonday(today) },
  ]}
  trigger={<Chip leading="calendar">{task.due ? formatShort(task.due) : 'Due date'}</Chip>}
/>
```

### Travel dates

`mode="range"` behind two fields that read as one. Done stays disabled until both dates are set.

<PhonePreview size="sm">
  <div class="ax-appbar"><span class="ax-icon-btn"><Glyph name="chevron-left" /></span><span class="ax-appbar-title">Porto · 2 guests</span><span class="ax-icon-btn" /></div>
  <div class="ax-body" style={{ gap: '12px' }}>
    <div class="ax-input-group" style={{ height: '60px' }}><div class="ax-grow" style={{ flexDirection: 'column', alignItems: 'flex-start', justifyContent: 'center', gap: '0px' }}><span class="ax-caption ax-muted">Check-in</span><span class="ax-semibold">Thu 17 Sep</span></div><div class="ax-grow" style={{ flexDirection: 'column', alignItems: 'flex-start', justifyContent: 'center', gap: '0px' }}><span class="ax-caption ax-muted">Check-out</span><span class="ax-semibold">Mon 21 Sep</span></div></div>
    <span class="ax-btn" data-block data-size="lg">Search stays</span>
    <span class="ax-footnote ax-muted" style={{ textAlign: 'center' }}>4 nights</span>
  </div>
</PhonePreview>

```tsx
<DatePicker
  mode="range"
  value={stay}
  onChange={setStay}
  minDate={today}
  calendarProps={{ minRange: 1 }}
  trigger={<StayField from={stay?.from} to={stay?.to} />}
/>
```

### Form field with validation

A regular field in a form, with `label`, `maxDate` and an error when the date is missing.

<PhonePreview size="sm">
  <div class="ax-appbar"><span class="ax-icon-btn"><Glyph name="close" /></span><span class="ax-appbar-title">Create account</span><span class="ax-icon-btn" /></div>
  <div class="ax-body" style={{ gap: '14px' }}>
    <div class="ax-field"><span class="ax-label">Full name</span><div class="ax-control"><span class="ax-value">Katherine Johnson</span></div></div>
    <div class="ax-field" data-state="invalid"><span class="ax-label">Date of birth</span><div class="ax-control"><span class="ax-placeholder">Select a date</span><Glyph name="calendar" size={20} /></div><span class="ax-helper">We need your date of birth to continue.</span></div>
  </div>
</PhonePreview>

```tsx
<DatePicker
  label="Date of birth"
  value={birthdate}
  onChange={setBirthdate}
  maxDate={subYears(today, 13)}
  error={submitted && !birthdate ? 'We need your date of birth to continue.' : undefined}
/>
```

## Related

- [Calendar](/atoms/calendar)
- [BottomSheet](/atoms/bottom-sheet)
